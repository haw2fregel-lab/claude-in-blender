schema_version = "1.0.0"

id = "claude_bridge"
version = "1.5.0"
name = "Claude Bridge"
tagline = "Send requests to your Claude Code session from inside Blender"
maintainer = "haw2fregel"
type = "add-on"
website = "https://github.com/haw2fregel/claude-in-blender"
tags = ["Development", "User Interface"]
blender_version_min = "4.2.0"
license = ["SPDX:GPL-3.0-or-later"]

[permissions]
network = "Local-only TCP bridge for MCP (binds 127.0.0.1)"
files = "Read the bridge session file in ~/.claude"
clipboard = "Copy Claude's reply"
